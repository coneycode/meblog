var console = hexo.extend.console;

var deployOptions = {
  alias: 'b',
  options: [
    {name: '--i', desc: 'Setup without deployment'},
    {name: '-g, --generate', desc: 'Generate before deployment'}
  ]
};

console.register('backup', 'Deploy your website', deployOptions, require('./backup'));